{
  "name": "dypiu-recruitment-backend",
  "version": "1.0.0",
  "description": "Backend API for DYPIU Recruitment Portal",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "npx nodemon src/index.js",
    "prisma:generate": "prisma generate --schema=../prisma/schema.prisma && node -e \"require('fs').cpSync('../node_modules/.prisma', 'node_modules/.prisma', {recursive: true})\"",
    "prisma:push": "prisma db push --schema=../prisma/schema.prisma",
    "db:seed": "node src/seed.js",
    "postinstall": "prisma generate --schema=../prisma/schema.prisma && node -e \"try { require('fs').cpSync('../node_modules/.prisma', 'node_modules/.prisma', {recursive: true}); } catch (e) {}\""
  },
  "dependencies": {
    "@prisma/client": "^5.12.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.12.1"
  }
}
